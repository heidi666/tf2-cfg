// Rebinds
exec config_default
exec autoexec

// Shotgun finish
alias +finisher slot2
alias -finisher lastinv
bind MOUSE2 +finisher

// Shift-click yes/no
alias +voice "bind MOUSE1 m1voice; bind MOUSE2 m2voice"
alias -voice "bind MOUSE1 +attack; bind MOUSE2 +finisher"
alias m1voice "voicemenu 0 6"
alias m2voice "voicemenu 0 7"
bind shift +voice

// Rocketjump
alias +bombjump "+jump; +duck; +attack"
alias -bombjump "-jump; -duck; -attack"
bind MOUSE3 +bombjump

// Teleport for rocketjump maps
bind o sm_cpsave
bind p sm_cptele

// Spam
bind DOWNARROW "say I will shit all over you and you will drown in it. You’re fucking dead, kiddo."

echo "||||| heidi soldier.cfg loaded |||||"
